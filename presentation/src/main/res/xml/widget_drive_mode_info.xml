<?xml version="1.0" encoding="utf-8"?>
<!--
  Request 3 columns and 1 row; actual cell sizes depend on the launcher.
  Keep the fallback height small to leave room for launcher-added padding.
-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:initialLayout="@layout/widget_drive_mode"
    android:minWidth="220dp"
    android:minHeight="40dp"
    android:minResizeHeight="40dp"
    android:targetCellWidth="3"
    android:targetCellHeight="1"
    android:resizeMode="vertical"
    android:updatePeriodMillis="0"
    android:widgetCategory="home_screen"
    android:description="@string/drive_mode_widget_desc" />
